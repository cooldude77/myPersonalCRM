{% if entities is not null and entities|length > 0 %}
    <div class="container">
        {% for entity in entities %}
            <div class="row">
                {% for column in listGrid.columns %}
                <div class="col-sm-4" style="background-color:lavender;">
                   hello
                </div>
                {%  endfor %}
            </div>
        {% endfor %}
    </div>
    {% else %}
    {% include 'admin/ui/panel/_create_button_in_list.html.twig' with {'params':listGrid.create_button}%}
{% endif %}

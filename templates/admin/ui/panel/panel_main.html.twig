{# templates/admin/ui/panel/panel_admin.html.twig #}
{% extends 'base/admin_base_template.html.twig' %}


{% block header %}

    {#
    Note: These two will give different result
    // This will not work as request is not direct
    {{ dump(app.request.query) }}
    // this will be used while forwarding
    // so always pass request as a parameter
    {{ dump(request.query) }}
    #}
    {{ render(controller(
        'App\\Controller\\Component\\UI\\Panel\\Components\\PanelHeaderController::header',{'request':request}
    )) }}
{% endblock %}


{% block sidebar %}
     {{ render(controller(
         'App\\Controller\\Component\\UI\\Panel\\Components\\PanelSideBarController::sideBar',{'request':request}
     )) }}
{% endblock %}


{% block content %}
    {{ render(controller(
        'App\\Controller\\Component\\UI\\Panel\\Components\\PanelContentController::content',{'request':request}
    )) }}

{% endblock %}
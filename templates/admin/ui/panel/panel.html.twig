{# templates/user/create.html.twig #}
{% extends 'base/my_base_template.html.twig' %}
  {% block javascripts %}
      <script src="https://unpkg.com/vue@3/dist/vue.global.js"></script>
  {% endblock %}
 {% block sidebar %}
     <ul>
         <li>Product</li>
         <li>
             <ul>
                 <li>
                     <a href="{{ path('admin_panel',{ 'load_next': 'product_list' }) }}"> List Products </a>
                 </li>
             </ul>
         </li>
         <li>Customer</li>
         <li>
             <ul>
                 <li>
                     <a href="{{ path('admin_panel',{ 'load_next': 'customer_list' }) }}"> List Customer </a>
                 </li>
             </ul>
         </li>
     </ul>
 {% endblock %}
{% block content %}
    <div id="app">(( message))</div>


    {{ content|raw }}

    <script type="module">

        const app = Vue.createApp({
            delimiters: ['((', '))'],
            data() {
                return {
                    message: "Hello World!"
                }
            }
        })

        app.mount('#app')

    </script>
{% endblock %}

<ul class="nav nav-pills flex-column">
    <li class="nav-item">
        <a href="{{ path('admin_panel') }}" class="nav-link align-middle px-0" id="panel-menu">
            <i class="fs-4 bi bi-house"></i> <span class="ms-1 d-none d-sm-inline">Home</span>
        </a>
    </li>
    {% for section in sections %}
        {% include  'base/_li_section_with_header.html.twig' with section %}
    {% endfor %}

</ul>
<div>

    <div>Logo</div>

    <div>{{ form(form,{'action': path('home')}) }}</div>

    {% if (app.user) %}
        <div><a href="{{ path('my_profile_panel') }}">Profile</a></div>
    {% endif %}

    {% if (app.user) %}
        <div><a href="{{ path('app_logout') }}">Logout</a></div>
    {% else %}
        <div><a href="{{ path('app_login') }}">Login </a></div>

    {% endif %}
</div>